# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 渲染中间产物
*.pv*.png
_build/
.build/

# 讲义产物不入库（内容源在 src/，可随时重渲染）
output/

# 真题语料库（corpus/）**随仓库分发**，clone 下来即可直接检索，因此不忽略。
# 仅排除下载残留与检索输出：
corpus/.tmp/
corpus/*.part
corpus/hits*.txt
